<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <Button
        android:id="@+id/profile_details_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Profile Details" />

    <Button
        android:id="@+id/create_listing_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Create Listing" />

    <Button
        android:id="@+id/messages_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Messages with Sellers" />

    <Button
        android:id="@+id/active_listings_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="View Active Listings"
        android:textColor="#FFFFFF"
        android:background="#0083FC"
        android:layout_marginTop="8dp" />

    <Button
        android:id="@+id/sold_listings_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="View Sold Listings"
        android:textColor="#FFFFFF"
        android:background="#0083FC"
        android:layout_marginTop="8dp" />

</LinearLayout>
