<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/create_listing_scroll_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <LinearLayout
        android:id="@+id/create_listing_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Existing UI elements -->

        <!-- Title -->
        <EditText
            android:id="@+id/title_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Title"
            android:inputType="text"
            android:background="#B3D9FF"
            android:textColor="#000000" />

        <!-- Price -->
        <EditText
            android:id="@+id/price_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Price"
            android:inputType="numberDecimal"
            android:background="#B3D9FF"
            android:textColor="#000000" />

        <!-- Description -->
        <EditText
            android:id="@+id/description_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Description"
            android:inputType="textMultiLine"
            android:lines="3"
            android:gravity="top"
            android:background="#B3D9FF"
            android:textColor="#000000" />

        <!-- Country -->
        <EditText
            android:id="@+id/country_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Country"
            android:inputType="text"
            android:background="#B3D9FF"
            android:textColor="#000000" />

        <!-- Postal Code -->
        <EditText
            android:id="@+id/postal_code_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Postal Code"
            android:inputType="text"
            android:background="#B3D9FF"
            android:textColor="#000000" />

        <!-- ImageView and Buttons -->
        <!-- ... existing code for image and buttons ... -->

        <!-- Save Button -->
        <Button
            android:id="@+id/save_listing_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Save Listing"
            android:textColor="#FFFFFF"
            android:background="#0083FC"
            android:layout_marginTop="16dp" />

    </LinearLayout>
</ScrollView>
